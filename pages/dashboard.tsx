import 'twin.macro';
import Head from 'next/head';
import Layout from '../components/Layout';

const Dashboard = (): JSX.Element => {
  return (
    <Layout>
      <h1>Dashboard</h1>
    </Layout>
  );
};
